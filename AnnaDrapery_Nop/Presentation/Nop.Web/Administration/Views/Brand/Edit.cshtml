@model BrandModel
@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Brands.EditBrandDetails").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "brand-form" }))
{
    @Html.AntiForgeryToken()
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Administration/Content/images/ico-catalog.png")" alt="" />
            @T("Admin.Catalog.Brands.EditBrandDetails") - @Model.Name @Html.ActionLink("(" + T("Admin.Catalog.Brands.BackToList") + ")", "List")
        </div>
        <div class="options">
            
            <input type="submit" name="save" class="k-button" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="k-button" value="@T("Admin.Common.SaveContinue")" />
            <span id="brand-delete" class="k-button">@T("Admin.Common.Delete")</span>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
@Html.DeleteConfirmation("brand-delete")
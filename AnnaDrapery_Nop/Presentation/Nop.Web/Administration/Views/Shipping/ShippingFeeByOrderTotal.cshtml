@{    
    //page title
    ViewBag.Title = T("Admin.Configuration.Shipping.ShippingFeeByOrderTotal").Text;
}
@Html.AntiForgeryToken()
<div class="section-header">
    <div class="title">
        <img src="@Url.Content("~/Administration/Content/images/ico-configuration.png")" alt="" />
        @T("Admin.Configuration.Shipping.ShippingFeeByOrderTotal")
    </div>
    <div class="options">
        <a href="@Url.Action("CreateShippingFeeByOrderTotal")" class="k-button">@T("Admin.Common.AddNew")</a>
    </div>
</div>
<table class="adminContent">
    <tr>
        <td>
            <div id="shippingmethod-grid"></div>

            <script>
                $(document).ready(function () {
                    $("#shippingmethod-grid").kendoGrid({
                        dataSource: {
                            type: "json",
                            transport: {
                                read: {
                                    url: "@Html.Raw(Url.Action("ShippingFeeByOrderTotal", "Shipping"))",
                                    type: "POST",
                                    dataType: "json",
                                    data: addAntiForgeryToken
                                }
                            },
                            schema: {
                                data: "Data",
                                total: "Total",
                                errors: "Errors"
                            },
                            error: function(e) {
                                display_kendoui_grid_error(e);
                                // Cancel the changes
                                this.cancelChanges();
                            },
                            serverPaging: true,
                            serverFiltering: true,
                            serverSorting: true
                        },
                        pageable: {
                            refresh: true,
                            numeric: false,
                            previousNext: false,
                            info: false
                        },
                        editable: {
                            confirmation: false,
                            mode: "inline"
                        },
                        scrollable: false,
                        columns: [{
                            field: "MinValue",
                            title: "@T("Admin.Configuration.Shipping.ShippingFeeByOrderTotal.Fields.MinValue")",
                            width: 200
                        }, {
                            field: "MaxValue",
                            title: "@T("Admin.Configuration.Shipping.ShippingFeeByOrderTotal.Fields.MaxValue")",
                            width: 400
                        },{
                            field: "ShippingFee",
                            title: "@T("Admin.Configuration.Shipping.ShippingFeeByOrderTotal.Fields.ShippingFee")",
                            width: 100
                        }, {
                            field: "Id",
                            title: "@T("Admin.Common.Edit")",
                            width: 100,
                            template: '<a href="EditShippingFeeByOrderTotal/#=Id#">@T("Admin.Common.Edit")</a>'
                        }]
                    });
                });
            </script>
        </td>
    </tr>
</table>
